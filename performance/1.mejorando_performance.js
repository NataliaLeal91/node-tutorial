// 1.- use node cluster module -> recomendado
// 2.- user worker threads -> experimental


// cluster manager (monitoreo de la salud de cada instancia)
  //start, stop, restart instancias. 
  // enviar datos
  // tareas administrativas

// single thread -> node server
// single thread -> node server
// single thread -> node server
